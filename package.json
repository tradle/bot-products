{
  "name": "@tradle/bot-products",
  "version": "1.0.0",
  "description": "bot that enables form-driven product purchases",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/tradle/bot-products"
  },
  "license": "MIT",
  "author": "mvayngrib",
  "dependencies": {
    "@tradle/bot-require-models": "^1.1.1",
    "@tradle/bots": "tradle/bots#dev",
    "@tradle/build-resource": "tradle/build-resource",
    "@tradle/models": "https://github.com/tradle/models",
    "@tradle/validate-model": "tradle/validate-model",
    "bluebird": "^3.4.7",
    "uuid": "^3.0.1",
    "xtend": "^4.0.1"
  },
  "peerDependencies": {},
  "devDependencies": {
    "eslint": "^3.15.0",
    "tape": "^4.6.3"
  },
  "scripts": {
    "lint": "eslint --quiet .",
    "start": "bots --conf ./conf.json",
    "test": "NODE_ENV=test DEBUG=tradle:bot* BLUEBIRD_DEBUG=1 node test",
    "test:debug": "NODE_ENV=test DEBUG=tradle:bot* BLUEBIRD_DEBUG=1 node --debug --inspect test",
    "test:debug-brk": "NODE_ENV=test DEBUG=tradle:bot* BLUEBIRD_DEBUG=1 node --debug-brk --inspect test"
  }
}
